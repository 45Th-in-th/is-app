specVersion: "v1beta"
serviceId: "is-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "is-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
